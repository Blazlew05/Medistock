import React, { useState } from "react";`nimport API from "./api";`nfunction App() {`n  const [monto] = useState(10000);`n  const gestionarPago = async () => {`n    try {`n      const res = await API.post("webpay/iniciar/", { monto });`n      const { token, url } = res.data;`n      const form = document.createElement("form"); form.method = "POST"; form.action = url;`n      const input = document.createElement("input"); input.type = "hidden"; input.name = "token_ws"; input.value = token;`n      form.appendChild(input); document.body.appendChild(form); form.submit();`n    } catch (e) { alert("Error al conectar con Transbank"); }`n  };`n  return (<div style={{ textAlign: "center", padding: "50px" }}><h1>MadiStock - Módulo de Caja</h1><button onClick={gestionarPago} style={{ padding: "10px 20px", fontSize: "16px", backgroundColor: "#0070e8", color: "white", border: "none", cursor: "pointer" }}>Pagar con Webpay Plus</button></div>);`n}`nexport default App;
